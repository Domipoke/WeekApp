import 'package:flutter/material.dart';

class DefaultAppBar extends AppBar {
  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: Text("RemoteController"),
    );
  }
}
